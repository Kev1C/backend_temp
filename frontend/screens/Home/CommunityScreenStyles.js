// frontend/screens/Home/CommunityScreenStyles.js

import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: '#fff', // Consider using theming for dynamic colors
  },
  text: {
    fontSize: 20,
    fontWeight: 'bold',
    color: '#000', // Default color; can be dynamic based on theme
  },
});

export default styles;